<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.speedata.device">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConfigAct"></activity>
        <activity android:name=".OperationActivity"></activity>
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AllGpiosAct"></activity>
        <activity android:name=".MainGpiosAct"></activity>
        <activity android:name=".helper.HelperActivity"></activity>
        <activity android:name=".I2CActivity"></activity>
    </application>

</manifest>